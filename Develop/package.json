{
  "name": "progressive-web-applications-pwa-challenge-text-editor-19",
  "version": "1.0.0",
  "description": "A text editor that runs in the browser.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}

